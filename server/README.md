# Multiplayer server

Run from the repository root with `npm run server`.

The server listens on `PORT` (default `8080`) and keeps rooms in memory for the current process. Production persistence, authentication, rate limiting and horizontal scaling are planned for later phases.
