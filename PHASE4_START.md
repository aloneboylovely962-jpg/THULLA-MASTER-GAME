# Phase 4 — Production Multiplayer

Started from the current `main` after Phase 3. This phase hardens the multiplayer server for hosted deployment, adds a health endpoint, WebSocket heartbeat, connection limits, origin controls, structured logging, and Railway service configuration.
